import { BrowserRouter, Navigate, Route, Routes } from 'react-router-dom'; import { QueryClient, QueryClientProvider } from '@tanstack/react-query'; import { Login } from './pages/Login'; import { Dashboard } from './pages/Dashboard'; import { useAuth } from './hooks/useAuth'; import './index.css';
const queryClient = new QueryClient();
function Protected() { const auth = useAuth(); if (auth.isLoading) return <div className="boot">Loading workspace...</div>; return auth.isError ? <Navigate to="/login" replace /> : <Dashboard />; }
export default function App() { return <QueryClientProvider client={queryClient}><BrowserRouter><Routes><Route path="/login" element={<Login />} /><Route path="/*" element={<Protected />} /></Routes></BrowserRouter></QueryClientProvider>; }