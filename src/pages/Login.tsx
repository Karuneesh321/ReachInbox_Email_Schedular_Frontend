import { Mail } from 'lucide-react'; import { apiBaseUrl } from '../services/api';
export function Login() { return <main className="login-page"><div className="login-art"><span className="eyebrow">Outbound, with intent</span><h1>Make every<br /><em>message count.</em></h1><p>A calm, reliable workspace for thoughtful email campaigns.</p></div><div className="login-panel"><div className="brand"><span className="brand-mark">R</span><span>ReachInbox</span></div><div className="login-copy"><span className="eyebrow">Welcome back</span><h2>Sign in to your workspace</h2><p>Connect your Google account to continue.</p><a className="google-button" href={`${apiBaseUrl}/auth/google`}><Mail size={18} /> Continue with Google</a></div><small className="login-foot">Secure authentication powered by Google OAuth</small></div></main>; }