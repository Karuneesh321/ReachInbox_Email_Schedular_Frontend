import type { User } from '../types/auth'; import { logout } from '../services/api'; import { useQueryClient } from '@tanstack/react-query'; import { LogOut } from 'lucide-react';
export function Header({ user }: { user: User }) { const queryClient = useQueryClient(); return <header><div className="brand"><span className="brand-mark">R</span><span>ReachInbox</span></div><div className="user"><img src={user.avatar || 'https://ui-avatars.com/api/?name=' + encodeURIComponent(user.name)} alt="" /><div><strong>{user.name}</strong><small>{user.email}</small></div><button className="logout" title="Log out" onClick={() => void logout().then(() => queryClient.clear())}><LogOut size={17} /></button></div></header>; }